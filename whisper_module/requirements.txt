# Whisper Module (ASR / Transcription) dependencies
# Developer B owns this file. Keep it independent from the xtts_module.

openai-whisper>=20231117
# Alternative faster backend (uncomment if you prefer faster-whisper):
# faster-whisper>=1.0.0

torch>=2.0.0
torchaudio>=2.0.0
ffmpeg-python>=0.2.0
numpy>=1.24.0
python-dotenv>=1.0.0
