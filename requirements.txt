# Root / shared dependencies (used by shared/ code: config, utils, pipeline).
# Module-specific ML dependencies live in each module's own requirements.txt.

pydub          # silence/energy detection for reference-clip extraction
librosa        # audio analysis helpers
soundfile      # reading/writing WAV audio
python-dotenv  # load settings from .env
